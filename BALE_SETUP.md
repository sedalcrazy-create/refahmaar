# راهنمای تنظیم بات بله - چالش مار یلدایی

## اطلاعات بات

- **نام بات:** @refahsnakebot
- **لینک:** ble.ir/refahsnakebot
- **توکن:** `64658763:rgYSwBxd05vEuuuNNbNNYZdtA-T1Gxdx5nw`
- **دامین:** snake.darmanjoo.ir

## مراحل تنظیم

### 1. تنظیم مینی‌اپ اصلی

1. به `@botfather` در بله بروید
2. بات خود را انتخاب کنید: @refahsnakebot
3. گزینه "Bot Settings" را انتخاب کنید
4. "Menu Button" را انتخاب کنید
5. "Configure menu button" را بزنید
6. URL را وارد کنید:
   ```
   https://snake.darmanjoo.ir
   ```
7. متن دکمه را تنظیم کنید:
   ```
   🎮 بازی یلدا
   ```

### 2. تنظیم دکمه Inline در پیام‌ها

برای افزودن دکمه بازی در پیام‌های کانال، از این کد استفاده کنید:

```python
from bale import Bot, InlineKeyboardMarkup, InlineKeyboardButton

bot = Bot(token="64658763:rgYSwBxd05vEuuuNNbNNYZdtA-T1Gxdx5nw")

# ساخت دکمه مینی‌اپ
keyboard = InlineKeyboardMarkup([
    [InlineKeyboardButton(
        text="🎮 شروع بازی یلدا",
        web_app={"url": "https://snake.darmanjoo.ir"}
    )]
])

# ارسال پیام در کانال
bot.send_message(
    chat_id="@your_channel",
    text="🍎 چالش مار یلدایی شروع شد!\n\nبا دوستان خود به مبارزه بپردازید و جوایز ویژه ببرید.\n\nیلدا مبارک! 🎉",
    reply_markup=keyboard
)
```

### 3. متن پیام پیشنهادی برای کانال

```
🎮 چالش مار یلدایی - ویژه کارکنان اداره کل رفاه و درمان

🗓 زمان مسابقه: یکشنبه ۳۰ آذر - از صبح تا آخر شب

🏆 جوایز ویژه برای برترین‌ها

چگونه شرکت کنم؟
1️⃣ روی دکمه "شروع بازی" کلیک کنید
2️⃣ نام، نام خانوادگی و کد استخدامی خود را وارد کنید
3️⃣ مار خود را کنترل کنید و غذا جمع کنید
4️⃣ از برخورد با دیوار و سایر بازیکنان اجتناب کنید

امتیازدهی:
🍎 سیب = ۱ امتیاز
🍉 هندوانه = ۵ امتیاز
🍇 انار = ۱۰ امتیاز

⭐️ فقط بالاترین امتیاز شما ثبت می‌شود

یلدا مبارک! 🌙
```

### 4. تنظیمات امنیتی (اختیاری)

برای محدود کردن دسترسی فقط به اعضای کانال:

```python
# در کد بات، اضافه کنید:
ALLOWED_CHANNEL = "@your_channel"

@bot.message_handler(commands=['start'])
def start(message):
    user_id = message.from_user.id

    # بررسی عضویت در کانال
    try:
        member = bot.get_chat_member(ALLOWED_CHANNEL, user_id)
        if member.status in ['member', 'administrator', 'creator']:
            # اجازه دسترسی
            pass
        else:
            bot.send_message(
                message.chat.id,
                "⚠️ برای شرکت در مسابقه ابتدا باید عضو کانال شوید."
            )
    except:
        pass
```

### 5. نمونه پیام خوش‌آمدگویی بات

```python
@bot.message_handler(commands=['start'])
def start(message):
    welcome_text = """
🎮 به چالش مار یلدایی خوش آمدید!

🗓 مسابقه: یکشنبه ۳۰ آذر
⏰ از صبح تا آخر شب

برای شروع بازی، روی دکمه منو (پایین صفحه) کلیک کنید.

یلدا مبارک! 🍎🍉
    """

    keyboard = InlineKeyboardMarkup([
        [InlineKeyboardButton(
            text="🎮 شروع بازی",
            web_app={"url": "https://snake.darmanjoo.ir"}
        )],
        [InlineKeyboardButton(
            text="🏆 رتبه‌بندی",
            callback_data="leaderboard"
        )]
    ])

    bot.send_message(
        message.chat.id,
        welcome_text,
        reply_markup=keyboard
    )
```

### 6. دستورات مفید بات

می‌توانید این دستورات را به بات اضافه کنید:

- `/start` - شروع بات و نمایش منو
- `/play` - لینک مستقیم بازی
- `/leaderboard` - نمایش برترین بازیکنان
- `/help` - راهنمای بازی
- `/rules` - قوانین مسابقه

### 7. نمایش Leaderboard در بات (اختیاری)

```python
@bot.callback_query_handler(func=lambda call: call.data == "leaderboard")
def show_leaderboard(call):
    # دریافت اطلاعات از API بازی
    import requests

    # فراخوانی API (باید endpoint اضافه کنید)
    # response = requests.get('https://snake.darmanjoo.ir/api/leaderboard')

    leaderboard_text = """
🏆 برترین بازیکنان

🥇 علی احمدی - ۱۲۳۴ امتیاز
🥈 مریم محمدی - ۱۱۵۰ امتیاز
🥉 حسین رضایی - ۱۰۲۰ امتیاز

به‌روزرسانی: لحظه‌ای
    """

    bot.answer_callback_query(call.id)
    bot.send_message(call.message.chat.id, leaderboard_text)
```

## تست و بررسی

### بررسی اتصال مینی‌اپ

1. به @refahsnakebot بروید
2. دستور `/start` را بزنید
3. روی دکمه منو کلیک کنید
4. بازی باید باز شود

### بررسی دامین

```bash
# بررسی SSL
curl https://snake.darmanjoo.ir/health

# بررسی WebSocket
wscat -c wss://snake.darmanjoo.ir/socket.io/
```

## عیب‌یابی

### مشکل: مینی‌اپ باز نمی‌شود

- بررسی کنید SSL فعال باشد (HTTPS ضروری است)
- بررسی کنید Nginx به درستی تنظیم شده باشد
- لاگ Nginx را چک کنید: `sudo tail -f /var/log/nginx/error.log`

### مشکل: بازی اجرا نمی‌شود

- بررسی کنید Docker container ها در حال اجرا باشند: `docker-compose ps`
- لاگ اپلیکیشن را چک کنید: `docker-compose logs -f app`

### مشکل: Bale SDK کار نمی‌کند

- بررسی کنید script tag در HTML صحیح باشد
- Console browser را چک کنید
- مطمئن شوید که از HTTPS استفاده می‌کنید

## اطلاعات تماس

در صورت مشکل:
- بررسی لاگ‌های سرور
- تست با مرورگر معمولی (https://snake.darmanjoo.ir)
- بررسی اتصال WebSocket

---

**آماده برای مسابقه یلدا! 🎮🍎**
