# 🎮 چالش مار یلدایی - Yalda Snake Challenge

بازی چند نفره Snake برای مسابقه شب یلدا - اداره کل رفاه و درمان

## 📋 مشخصات پروژه

- **تاریخ مسابقه:** یکشنبه 21 دسامبر 2024 (30 آذر 1403)
- **زمان:** از صبح تا آخر شب
- **پلتفرم:** مینی‌اپ بله (Bale Messenger)
- **حداکثر بازیکنان همزمان:** 100-150 نفر

## 🚀 نصب و راه‌اندازی

### پیش‌نیازها

- Docker & Docker Compose
- سرور با حداقل 4GB RAM و 4 Core CPU

### راه‌اندازی با Docker

1. کلون کردن پروژه:
```bash
git clone <repository-url>
cd marrefah
```

2. تنظیم Environment Variables:
```bash
cp .env.example .env
# ویرایش .env و تنظیم PASSWORD و سایر موارد
```

3. اجرای پروژه:
```bash
docker-compose up -d
```

4. بررسی وضعیت:
```bash
docker-compose ps
docker-compose logs -f app
```

5. دسترسی به بازی:
```
http://your-server-ip:3001
```

### توقف سرویس

```bash
docker-compose down
```

### حذف کامل (همراه با دیتابیس):
```bash
docker-compose down -v
```

## 🏗️ معماری

```
marrefah/
├── app/
│   ├── controllers/     # Game controller
│   ├── models/          # Player, Food, Coordinate models
│   ├── services/        # Game logic services
│   └── configs/         # Configuration files
├── database/
│   ├── init.sql         # Database schema
│   ├── db.js            # PostgreSQL connection
│   └── userService.js   # User & leaderboard operations
├── public/
│   ├── index.html       # Main HTML
│   ├── css/             # Styles
│   └── js/              # Client-side JavaScript
├── docker-compose.yml   # Docker configuration
├── Dockerfile           # App container
└── app.js              # Main server file
```

## 🎯 ویژگی‌ها

- ✅ احراز هویت با Bale SDK (شماره موبایل + نام + کد استخدامی)
- ✅ بازی Multiplayer real-time با Socket.io
- ✅ Leaderboard با PostgreSQL
- ✅ تم یلدایی (🍎 سیب، 🍉 هندوانه، 🍇 انار)
- ✅ طراحی Responsive (موبایل و دسکتاپ)
- ✅ کاملاً ایزوله در Docker

## 🎮 نحوه بازی

1. ورود از طریق بات بله
2. ثبت‌نام با نام، نام خانوادگی و کد استخدامی
3. کنترل مار با کیبورد (← ↑ → ↓) یا دکمه‌های لمسی
4. خوردن غذا:
   - 🍎 سیب = 1 امتیاز
   - 🍉 هندوانه = 5 امتیاز
   - 🍇 انار = 10 امتیاز
5. از برخورد با دیوار و سایر بازیکنان اجتناب کنید
6. بالاترین امتیاز شما ثبت می‌شود

## 📊 دیتابیس

### جدول users
- شناسه Bale
- شماره موبایل
- نام و نام خانوادگی
- کد استخدامی

### جدول leaderboard
- امتیاز
- طول مار
- مدت زمان بازی

## 🔧 تنظیمات

تمام تنظیمات در فایل `.env`:

- `PORT`: پورت سرور (پیش‌فرض: 3001)
- `MAX_PLAYERS`: حداکثر بازیکنان (پیش‌فرض: 150)
- `GAME_SPEED`: سرعت بازی FPS (پیش‌فرض: 10)
- `BOARD_WIDTH`, `BOARD_HEIGHT`: اندازه صفحه بازی

## 🐛 عیب‌یابی

### بررسی لاگ‌ها:
```bash
docker-compose logs -f app
docker-compose logs -f postgres
```

### دسترسی به Container:
```bash
docker exec -it yalda_snake_app sh
docker exec -it yalda_snake_db psql -U snake_user -d yalda_snake
```

### بررسی Health:
```bash
curl http://localhost:3001/health
```

## 📝 یادداشت‌ها

- سرور باید روی پورت 3001 در دسترس باشد
- برای استفاده در Bale، باید دامنه HTTPS داشته باشید
- Nginx می‌تواند برای reverse proxy استفاده شود

## 🎉 موفقیت‌آمیز!

بازی آماده است. برای اتصال به Bale:
1. بات را در @botfather ثبت کنید
2. مینی‌اپ را فعال کنید
3. URL سرور را تنظیم کنید
4. دکمه بازی را در پیام‌ها اضافه کنید

---

**یلدا مبارک! 🍎🍉**
