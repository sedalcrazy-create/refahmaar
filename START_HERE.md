# 🚀 شروع اینجا - Yalda Snake Challenge

## خلاصه سریع

پروژه **چالش مار یلدایی** برای مسابقه شب یلدا آماده است!

- **دامین:** snake.darmanjoo.ir
- **بات:** @refahsnakebot
- **تاریخ:** یکشنبه ۳۰ آذر ۱۴۰۳ (۲۱ دسامبر ۲۰۲۴)

## 📁 فایل‌های مهم

### برای Deploy:
- ✅ `DEPLOYMENT_GUIDE.md` - راهنمای کامل deploy (مرحله به مرحله)
- ✅ `quick-start.sh` - اسکریپت نصب سریع (یک دستور!)
- ✅ `deploy.sh` - اسکریپت deploy کامل
- ✅ `nginx-snake-darmanjoo.conf` - تنظیمات Nginx آماده

### برای تنظیم بله:
- ✅ `BALE_SETUP.md` - راهنمای کامل تنظیم بات بله

### مستندات:
- ✅ `README.md` - توضیحات کلی پروژه
- ✅ `CLAUDE.md` - راهنمای کدنویسی برای Claude

### فایل‌های تنظیمات:
- ✅ `.env` - تنظیمات محیطی (رمز دیتابیس، پورت، و...)
- ✅ `docker-compose.yml` - تنظیمات Docker
- ✅ `Dockerfile` - ساخت image برنامه

## 🎯 سه گام ساده برای شروع:

### گام 1️⃣: Deploy روی سرور (10 دقیقه)

**روش سریع (توصیه می‌شود):**

```bash
# اتصال به سرور
ssh root@37.152.174.87

# رفتن به پوشه پروژه
cd /opt/yalda-snake

# اجرای اسکریپت نصب سریع
chmod +x quick-start.sh
./quick-start.sh
```

**یا روش دستی:**

مطالعه کامل: [DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md)

### گام 2️⃣: تنظیم بات بله (5 دقیقه)

1. به @botfather در بله بروید
2. بات @refahsnakebot را انتخاب کنید
3. Menu Button را تنظیم کنید:
   - URL: `https://snake.darmanjoo.ir`
   - Text: `🎮 بازی یلدا`

راهنمای کامل: [BALE_SETUP.md](BALE_SETUP.md)

### گام 3️⃣: تست و راه‌اندازی (5 دقیقه)

```bash
# تست Health
curl https://snake.darmanjoo.ir/health

# باز کردن در مرورگر
# https://snake.darmanjoo.ir

# تست از بات بله
# به @refahsnakebot بروید و روی منو کلیک کنید
```

## ✅ چک‌لیست آماده‌سازی

قبل از لانچ مسابقه، این موارد را بررسی کنید:

- [ ] سرور در حال اجرا است: `docker-compose ps`
- [ ] SSL فعال است: `https://snake.darmanjoo.ir` باز می‌شود
- [ ] Health check سبز است: `/health` پاسخ OK می‌دهد
- [ ] بازی لود می‌شود و Canvas نمایش داده می‌شود
- [ ] ثبت‌نام کار می‌کند (نام، کد استخدامی)
- [ ] بازی قابل کنترل است (کیبورد یا touch)
- [ ] امتیازها ذخیره می‌شوند
- [ ] Leaderboard نمایش داده می‌شود
- [ ] بات بله به بازی لینک می‌شود
- [ ] مینی‌اپ در بله باز می‌شود

## 🆘 مشکل پیش آمد؟

### خطای Nginx
```bash
# بررسی لاگ
tail -f /var/log/nginx/error.log

# ری‌استارت
systemctl restart nginx
```

### خطای Docker
```bash
# بررسی وضعیت
docker-compose ps

# مشاهده لاگ
docker-compose logs -f app

# ری‌استارت
docker-compose restart
```

### خطای دیتابیس
```bash
# ورود به PostgreSQL
docker exec -it yalda_snake_db psql -U snake_user -d yalda_snake

# بررسی جداول
\dt

# تست کوئری
SELECT COUNT(*) FROM users;
```

## 📊 دستورات مفید

### مانیتورینگ
```bash
# لاگ زنده
docker-compose logs -f app

# وضعیت سرورها
docker-compose ps

# مصرف منابع
docker stats
```

### مدیریت
```bash
# ری‌استارت
docker-compose restart app

# توقف
docker-compose down

# شروع مجدد
docker-compose up -d
```

### دیتابیس
```bash
# تعداد کاربران
docker exec yalda_snake_db psql -U snake_user -d yalda_snake -c "SELECT COUNT(*) FROM users;"

# برترین بازیکنان
docker exec yalda_snake_db psql -U snake_user -d yalda_snake -c "SELECT * FROM high_scores LIMIT 10;"
```

## 📞 پشتیبانی

اگر مشکلی پیش آمد:

1. ابتدا لاگ‌ها را بررسی کنید
2. راهنمای عیب‌یابی در `DEPLOYMENT_GUIDE.md` را مطالعه کنید
3. بررسی کنید که تمام سرویس‌ها اجرا هستند

## 🎉 آماده برای مسابقه!

پروژه کاملاً آماده است. بعد از Deploy:

1. ✅ بازی آنلاین است
2. ✅ کاربران می‌توانند ثبت‌نام کنند
3. ✅ امتیازها ذخیره می‌شوند
4. ✅ Leaderboard کار می‌کند
5. ✅ بات بله متصل است

**یلدا مبارک! 🍎🍉🍇**

---

## 📚 منابع اضافی

- [PRD اصلی](prd_بازی_آنلاین_snake_یلدایی_اداره_کل_رفاه_و_درمان.md)
- [اطلاعات سرور](SERVER_SETUP_COMPLETE.md)
- [توکن بات](bale.txt)

---

**سوال یا مشکلی هست؟ فایل‌های راهنما را مطالعه کنید یا با Claude صحبت کنید! 🤖**
